rules:
  # Default type for plain messages (no /task prefix)
  generic:
    executors:
      - executor: claude_code
        executor_model: sonnet

  new_instance:
    executors:
      - executor: claude_code
        executor_model: sonnet
