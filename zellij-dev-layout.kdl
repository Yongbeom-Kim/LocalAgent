layout {
    tab split_direction="horizontal" name="LocalAgent Dev" focus=true {
        // Top row: infrastructure + core services
        pane split_direction="vertical" size="50%" {
            pane name="RabbitMQ" size="20%" {
                command "docker"
                args "compose" "up" "rabbitmq"
            }
            pane name="Migrator" size="20%" {
                command "npm"
                args "run" "dev" "--prefix" "packages/migrator"
            }
            pane name="API" size="20%" {
                command "npm"
                args "run" "dev" "--prefix" "packages/api"
            }
            pane name="Enrichment" size="20%" {
                command "npm"
                args "run" "dev" "--prefix" "packages/daemon/task-enrichment"
            }
            pane name="Task Daemon" size="20%" {
                command "npm"
                args "run" "dev" "--prefix" "packages/daemon/task"
            }
        }
        // Bottom row: notification daemons + scratch terminal
        pane split_direction="vertical" size="50%" {
            pane name="Lark Result" size="20%" {
                command "npm"
                args "run" "dev" "--prefix" "packages/daemon/lark-result"
            }
            pane name="Lark Listener" size="20%" {
                command "npm"
                args "run" "dev" "--prefix" "packages/daemon/lark-listener"
            }
            pane name="Telegram Inbound" size="20%" {
                command "npm"
                args "run" "dev" "--prefix" "packages/daemon/telegram-inbound"
            }
            pane name="Telegram Outbound" size="20%" {
                command "npm"
                args "run" "dev" "--prefix" "packages/daemon/telegram-outbound"
            }
            pane name="Shell" size="20%" focus=true
        }
    }
}
